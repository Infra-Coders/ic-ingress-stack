apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx-release
  namespace: ingress-nginx
spec:
  interval: 15m
  timeout: 5m
  releaseName: ingress-dev
  values:
    controller:
      replicaCount: 1
